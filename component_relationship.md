# 奇畅考拉核心组件关系

## 1. 页面结构与导航关系

### 1.1 页面列表
- **首页** (`pages/index/index`): 材料选择与购物车管理
- **订单页** (`pages/order/order`): 订单生成与导出
- **个人中心** (`pages/mine/mine`): 用户登录与历史订单管理

### 1.2 导航关系
```
┌──────────────┐     ┌──────────────┐
│              │     │              │
│   首页       │────▶│   订单页     │
│              │     │              │
└──────────────┘     └──────────────┘
        ▲                     │
        │                     │
        │                     ▼
┌──────────────┐     ┌──────────────┐
│              │     │              │
│   个人中心   │◀────│   历史订单   │
│              │     │              │
└──────────────┘     └──────────────┘
```

### 1.3 TabBar配置
- **首页**：材料选购入口
- **个人中心**：用户信息与历史订单

## 2. 核心功能模块与组件关系

### 2.1 首页模块
**核心组件**：
- `notice-bar`：透明底轮播通知栏
- `banner-swiper`：图片轮播组件
- `material-selector`：材料类别选择器
- `cart-manager`：购物车管理组件

**数据流向**：
```
材料选择器 → 购物车数据 → 本地存储 → 订单页
```

### 2.2 订单模块
**核心组件**：
- `order-info`：订单信息展示
- `address-selector`：地址选择器
- `save-option-modal`：保存选项弹窗
- `canvas-generator`：图片生成组件
- `excel-generator`：Excel生成组件

**数据流向**：
```
购物车数据 → 订单信息 → 地址选择 → 导出为图片/Excel
```

### 2.3 个人中心模块
**核心组件**：
- `login-form`：登录表单
- `user-info`：用户信息展示
- `menu-list`：功能菜单
- `order-history`：历史订单列表

**数据流向**：
```
登录信息 → 用户状态 → 历史订单 → 订单详情
```

## 3. 核心数据模型

### 3.1 材料数据模型
```json
{
  "materialType": "管材类",
  "brand": "中财",
  "category": "电线预埋管",
  "model": "215",
  "color": "白色",
  "num": "1",
  "unit": "米",
  "price": "10",
  "remark": ""
}
```

### 3.2 订单数据模型
```json
{
  "orderNo": "20251212174500",
  "userName": "冯祥君",
  "cart": [], // 包含多个材料对象
  "address": "上海市浦东新区",
  "createTime": 1734001500000
}
```

### 3.3 用户数据模型
```json
{
  "name": "冯祥君",
  "isLogin": true
}
```

## 4. 核心组件交互流程图

```
用户进入首页
  │
  ▼
查看轮播通知和图片
  │
  ▼
选择材料类别和属性
  │
  ▼
填写数量、单位和单价
  │
  ▼
添加到购物车
  │
  ├─────────────────────────────────────────┐
  ▼                                         ▼
继续添加材料                          点击"立即下单"
  │                                         │
  ▼                                         ▼
购物车管理                             生成订单
                                            │
                                            ▼
                                        选择配送地址
                                            │
                                            ▼
                                        保存为图片或Excel
                                            │
                                            ▼
                                        返回首页
                                            │
                                            ▼
                                        进入个人中心
                                            │
                                            ▼
                                        查看历史订单
```

## 5. 技术依赖与核心库

### 5.1 系统API依赖
- `wx.getStorageSync`/`wx.setStorageSync`：本地存储管理
- `wx.getDeviceInfo`：设备信息获取（用于HarmonyOS适配）
- `wx.canvasToTempFilePath`：Canvas图片生成
- `wx.saveImageToPhotosAlbum`：图片保存到相册
- `wx.getFileSystemManager`：文件系统操作

### 5.2 第三方库
- `xlsx.min.js`：Excel文件生成与导出

## 6. 鸿蒙系统适配组件关系

### 6.1 适配层设计
```
┌─────────────────────────┐
│                         │
│   应用层 (业务逻辑)     │
│                         │
└─────────────────────────┘
            ▲
            │
            ▼
┌─────────────────────────┐
│                         │
│   适配层 (设备检测)     │
│   - getDeviceInfo()     │
│   - isHarmonyOS         │
│                         │
└─────────────────────────┘
            ▲
            │
            ▼
┌─────────────────────────┐
│                         │
│   系统层 (微信/鸿蒙)     │
│                         │
└─────────────────────────┘
```

### 6.2 适配组件
- **设备信息检测**：通过`getDeviceInfo()`函数检测设备类型
- **状态栏高度适配**：使用`var(--status-bar-height)`变量动态调整UI位置
- **文件路径适配**：根据不同系统设置不同的文件存储路径
- **样式兼容性**：使用兼容的CSS属性，避免使用平台特定样式

## 7. 核心组件功能表

| 组件名称 | 所在页面 | 核心功能 | 依赖关系 |
|---------|---------|---------|---------|
| 轮播通知栏 | 首页 | 展示公司介绍 | swiper组件 |
| 图片轮播 | 首页 | 展示广告图片 | swiper组件 |
| 材料选择器 | 首页 | 选择材料类别和属性 | picker组件 |
| 购物车管理 | 首页 | 管理购物车商品 | 本地存储 |
| 订单生成 | 订单页 | 生成和展示订单 | Canvas、SheetJS |
| 地址选择器 | 订单页 | 选择和管理配送地址 | 本地存储 |
| 登录表单 | 个人中心 | 用户登录验证 | 本地存储 |
| 历史订单 | 个人中心 | 展示历史订单 | 本地存储 |

## 8. 数据存储关系

```
┌─────────────────────────┐
│                         │
│   用户信息 (userInfo)   │
│                         │
└─────────────────────────┘
        ▲
        │
┌───────┴───────┐
│               │
│  购物车数据   │
│     (cart)    │
│               │
└───────┬───────┘
        │
┌───────┴───────┐
│               │
│  历史订单     │
│ (historyOrders)│
│               │
└───────┬───────┘
        │
┌───────┴───────┐
│               │
│  地址列表     │
│ (addressList) │
│               │
└───────────────┘
```

## 9. 组件生命周期关系

```
应用启动
  │
  ▼
app.onLaunch()
  │
  │───初始化本地存储
  │───获取设备信息(HarmonyOS适配)
  │
  ▼
页面加载
  │
  │───首页 onLoad() → 加载购物车
  │───订单页 onLoad() → 生成订单号
  │───个人中心 onLoad() → 加载用户信息
  │
  ▼
页面显示
  │
  │───首页 onShow() → 重新加载购物车
  │───订单页 onShow() → 刷新订单数据
  │───个人中心 onShow() → 刷新用户信息和历史订单
  │
  ▼
用户交互
  │
  │───事件处理 → 更新数据 → 更新视图
  │
  ▼
页面跳转/关闭
  │
  ▼
应用退出
```

## 10. 功能模块与组件对应关系

| 功能模块 | 对应组件 | 所在页面 |
|---------|---------|---------|
| 材料选购 | 材料选择器、购物车管理 | 首页 |
| 订单管理 | 订单生成、地址选择器 | 订单页 |
| 文件导出 | Canvas生成器、Excel生成器 | 订单页 |
| 用户管理 | 登录表单、用户信息展示 | 个人中心 |
| 历史订单 | 历史订单列表、订单详情 | 个人中心 |
| 系统适配 | 设备检测、状态栏适配 | 全局 |

# 总结

奇畅考拉小程序采用了清晰的组件化架构，核心功能模块之间的关系明确，数据流转顺畅。通过本地存储实现了数据持久化，支持多页面间的数据共享。同时，针对HarmonyOS平台进行了适配，确保在不同设备上都能正常运行。

小程序的核心组件关系体现了"材料选择→购物车管理→订单生成→导出保存"的完整业务流程，为用户提供了便捷的材料采购解决方案。